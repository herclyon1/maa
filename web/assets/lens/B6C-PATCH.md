# B6-c patch — the lifted segment lens's edge lines (ui2, 2026-09-19 11:5x; for the ui session's view.js / index.html segment block)

Order (监督局 11:1x): the four changes of `predict-seg-hold.md` §6 with `keyfill-highlight.md` §2 / §5.1 / §5.2c. This file = the asset
(`#seg-lens-f-ish` in `lens-filter.svg`, generated by `gen_lens_maps.py` → `filter_inner_shadow`) + the patch text for the ui session's
files, which ui2 does not edit. Line numbers = `web/view.js` / `web/index.html` at origin/main (the segment block is unchanged between b25faed and the 11:02 发布 commit; only the `?v=` strings differ).
Judge = `predict-seg-hold.md` §6.1 (native = `seg-native-dragmid-light-full.png` 3×, rows in 3-px rows).

## 1 `k 去压缩` — the built-in KeyFill's amount uniform (keyfill §5.1 B6-3)

Read: the CPU loads `key_fill_highlight_amount` = 1/inputKeyFillHighlightAmount − 2 (render 0x1c399425c–0x1c3994264: d13 = 1/[key 0x197],
d15 = −2; 0x1c3994378 fadd → [sp+0x200] → 0x1c3994730 store at uniform +280); the lens's key .5 → uniform **0** → key′ = v·ang / (1 + 0·(1 − v·ang))
= v·ang, no compression (the same mapping as the #36 layer's SDFNode::apply).

| file:line | old | new | source |
|---|---|---|---|
| view.js:1305 | `g = (x) => x / (1 + .5 * (1 - x))` | `KF_AMOUNT = 1 / .5 - 2` (= 0), `g = (x) => x / (1 + KF_AMOUNT * (1 - x))` | keyfill §5.1 B6-3 |
| view.js:1303 (comment) | `Amount .5 (x′ = x/(1 + .5(1 − x)))` | `Amount .5 → uniform 1/.5 − 2 = 0 (x′ = x)` | same |
| index.html:584 (comment) | `x′ = x/(1 + .5(1 − x))` | `x′ = x (uniform amount 1/.5 − 2 = 0, keyfill §5.1 B6-3)` | same |
| index.html:586–592 (comment) | the "Known residual … 7–9 levels lighter … (a) whether the CPU loads the Amount uniform as 1/amount − 2" paragraph | delete — (a) is read and is the whole difference (old table −19.3 / −22.3 → −26.5 / −26.5) | predict §6.1 rows 1–3 |

Expected after (predict §6.1): row 601 px rows 601.5 / 601.83 / 602.17 = **197 / 211 / 224** (mean 217.2 = −26.5; native −26.6), row 646 =
−26.5 (KeyFill) − 3.3 (ring-shadow tail) = **−29.8** (native −29.9), row y616 x110 + x111 = **−53.1** (native −53; the split between the two
columns is the arc's sub-pixel phase). With g = identity the tip value `kfK(v, −1)` = v (.875 / .625 / .375) and the top/bottom value
`kfK(v, 0)` = 2v/3 (.583 / .417 / .250) — exactly the k of §5.1's table.

## 2 `fwidth AA 坡 ±⅙ pt` — the band's anti-aliasing (keyfill §2 B6-2)

Read: aa = saturate(e/fw + .5) · saturate((h − e)/fw + .5), fw = fwidth(e) per FRAGMENT pixel (IR %37 `air.fwidth.f32(%29)`), i.e. fw =
(|n_x| + |n_y|)/contentsScale pt: ⅓ pt on the straight edges at 3×, .471 pt on the 45° arc; the shader evaluates at the pixel CENTRE. Hence on
the straight edges the ramp (e ∈ [−⅙, ⅙] and [⅚, 7⁄6]) ends exactly at the neighbouring pixel centres: px rows 601.17 / 601.5 / 601.83 / 602.17 /
602.5 = **244 / 197 / 211 / 224 / 244** — the same values as the hard-edged rings sampled at their centres (a 1-CSS-px ramp would put 601.17 at
~226: aa(e = −⅙) = ⅓ → k .222 → 244·(1 − .3266·.222)). On the arcs the coverage anti-aliasing of a geometric stroke IS a linear ramp of width
(|n_x| + |n_y|) device px along the normal (box-filter coverage of a tilted edge) — the same as fwidth — so the ring strokes need no extra ring;
what they need is to be rasterised at the DEVICE scale with the default `shape-rendering` (not crispEdges) and the ring edges on e = 0 / 1 (depth
−⅔ / +⅓ from the zero line, view.js:1307 `kfRings` — already so). One change:

| file:line | old | new | source |
|---|---|---|---|
| index.html:603–606 (comment) | "the shader's fwidth antialiasing term … is not drawn because the unit of fw on this layer … is unread" | "fw = (|n_x| + |n_y|)/contentsScale pt (⅓ pt straight, .471 on the arc at 3×), sampled at pixel centres → on the straight edges identical to the hard ⅓-pt rings, on the arcs = the rasteriser's coverage AA of the strokes" | keyfill §2 B6-2, predict §6.1 row 4 |
| view.js:1307 `kfRings` | unchanged | unchanged (three ⅓-pt rings at depth −⅔ … ⅓, v .875 / .625 / .375) | §5.1 prof at the row centres e = ⅙ / ½ / ⅚ |

Check on the device: `?segx=noab` vs default — the rings sit inside `.stack`, under the fringe chain's software filter; if that buffer were at 1×
the rows would smear to a 1-CSS-px ramp (601.17 → ~226): README §0.4's engine fact says the composited path's buffer is at the device scale
(wksnap @3 read ⅙ pt). The Mac 2× (wksnap) cannot show these rows right by construction (⅓-pt rings on ½-pt pixels) — the device is the judge.
Residual on the arcs (predict §6.1 row 5): the shader's inner tail at e ∈ (1, 1 + fw/2) (prof .25 for e > h, §5.1: mix(1, 0, .75)) is not
what coverage AA of a ring ending at e = 1 gives (a ramp from .375 to 0): ≈ 7/255 vs ≈ 9/255 one pixel inside the arc — within the row's
tolerance ("差 4"), recorded, not patched.

## 3 `白栈外溢一行` — the #36 highlight's white stack outside the capsule (predict §6.1 rows 7–8)

Native: nothing outside the capsule (row 602 x 108–123 and row 603 x 108–120 = the page 242 / 242 / 247 to the pixel); the KeyFill band is
e ≥ −fw/2 (≤ ⅙ pt outside on the straight edges, 0 at the pixel centres; ≤ .24 pt on the arcs) and the layer is drawn inside the lens's clip.
Page: `.hlw` (white, `mix-blend-mode: plus-lighter`) and `.hlk` are `<svg>` elements with the lens box + 12 px margin (view.js:1451) and
`overflow: visible` (index.html:572), so whatever the ring strokes' gradients / anti-aliasing put outside the capsule is added to the page.

| file:line | old | new | source |
|---|---|---|---|
| index.html:558 `.segctl .hlk,.segctl .hlw{…}` | no clip | append `clip-path:inset(12px round var(--rr, 14px))` — the capsule itself (the SVG's box is the lens box + 12 px) | predict §6.1 rows 7–8; keyfill §2 aa (nothing beyond fw/2) |
| view.js:1451 (the `hls` box loop, SVG branch) | sets left/top/width/height only | also `el.style.setProperty("--rr", R + "px")` (the clip's radius; the HLCSS branch already sets it) | same |
| index.html:3925–3928 `?segx` switches | — | add `hlclip0` → `.segctl .hlk,.segctl .hlw{clip-path:none}` (the before/after switch for the judge) | — |

Expected after: row 602 x 108–123 = page (242 / 242 / 247), row 603 x 108–120 = page; the light line's own rows unchanged (the clip is at the
zero line, the band's first ring starts at depth 0).

## 4 Inner shadow #21 — the band construction (keyfill §5.2c, predict §0d)

Read (QuartzCore `emit_shadow_path` 0x1c3ad0d3c; closed form fixed by the data session's four sdfset variants): α(p) = shadowOpacity · M(p) ·
blur_σ(M − M↓offset)(p), M = the capsule mask, σ = shadowRadius (pt; `gaussian_vimage_8`, predict §0d), offset a float, no other factor on
opacity — "the shape minus its own copy moved down by the offset, blurred, clipped back to the shape". NOT the complement blurred (CSS
`box-shadow: inset`): that construction gives .057–.059 on the top rows where the raster reads .033 / .039 (measured in WebKit on the same
page: inset 0 7px 6px .06 → rows 302…313 = .057 .057 .055 .052 .048 .044 .036 .026 .018 .010 .004 .001).

The asset: `lens-filter.svg` → `#seg-lens-f-ish` (`gen_lens_maps.py` `filter_inner_shadow`; `data-op .06`, `data-offset 7`, `data-sigma 3`):
feOffset(SourceAlpha, dy 7) → feComposite out (M − M↓7) → feGaussianBlur σ 3 → feComposite in (× M) → feComponentTransfer α × .06 → black.
Apply it to a black capsule of the lens box. WebKit check (wksnap 3 px/pt, page 244, column x 220, α = 1 − v/244):

| row | 602 | 603 | 604 | 605 | 606 | 607 | 608 | 609 | 610 | 611 | 612 | 613 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| §5.2c closed form (= native A within .004 from 603) | .033 | .039 | .044 | .045 | .044 | .039 | .033 | .026 | .019 | .012 | .007 | .004 |
| `#seg-lens-f-ish` in WebKit | .034 | .038 | .044 | .046 | .044 | .038 | .033 | .025 | .016 | .008 | .003 | .000 |

602–609 within .001; the tail 610–613 reads one 8-bit level low (the blur's 8-bit α × .06 = 15 levels; the native raster is 8-bit too, its
tail reads .005 at 613). Bottom rows 640–648: 0 (as native: "下缘 624 以下 < .004"). Left end, row 616, x 111…121: .012 .025 .029 .025 .025
.020 .016 .012 .008 .004 .000 — the data session's direct read of the ends (12:5x, `tools/touch/ish_ends.py`, seg-sdfexp9 on / hidden pairs,
three backdrops): row 616 left x 114–116 / right x 324–326 peak α **.025** (3–6 pt inside the edge, gone 10 pt in), row 606 plateau .045
(x 122 … 321), row 626 ends ≤ .01; the parent chain's opacities all 1, only #21 has a shadow. The filter's end peak .029 at x 113 vs .025:
within .004, as on the top-edge rows — the closed form holds at the ends too (§5.2's "+3.8 … +1.9" was the coarser earlier read).

| file:line | old | new | source |
|---|---|---|---|
| index.html:570 `.segctl .rimb .ish,.segctl .rimo .ish{inset:0;border-radius:inherit;box-shadow:inset 0 calc(8px * var(--lp, 0)) calc(6px * var(--lp, 0)) 0 rgb(0 0 0 / .06)}` | inset box-shadow, offset 8, blur 6 | `{inset:0;border-radius:inherit;background:#000;filter:url(#seg-lens-f-ish)}` — the element = the lens box (top 602), offset 7 in the filter | keyfill §5.2c (y_top = 602, off 7, σ 3, op .06 — the table's rows) |
| index.html:561–569 (comment) | "invertsShadow = the shape's complement blurred, translated by the offset, clipped by the shape — CSS box-shadow: inset is the same construction … on this element (the lens box, top 602) the offset is 8 … the reading .06 is drawn; the raster reads .043 — unresolved" | "§5.2c: op · M · blur_σ(M − M↓7); the plateau .045 = .06 × .758 (the 7-pt band eaten 24 % by the σ-3 kernel) — the .043 is not a factor; drawn by #seg-lens-f-ish on the lens box, offset 7" | keyfill §5.2c |
| index.html (the inline copy of lens-filter.svg, after the last `#seg-lens-f-ab-ir-256` filter, before `</svg>`) | — | paste `#seg-lens-f-ish` (10 lines, lens-filter.svg lines 3129–3138) | this asset |
| view.js `frame(p, pd)` (the lift ramp, §1c(b): offset / opacity / radius on one curve) | the CSS calc(× --lp) on offset and blur | per frame: `feOffset dy = 7·lp`, `feGaussianBlur stdDeviation = 3·lp`, `feFuncA slope = .06·lp` on `#seg-lens-f-ish` (or keep the element's `opacity: var(--lp)` only — the geometry ramp is the 0.25-s lift) | seg-lens-refraction §1c(b) |

`?segx=noish` keeps working (it hides `.ish`).

## 4b `主带只画直边` — the #36 main band on the straight runs only (predict §6.1 rows 5–6; 验收 11:5x)

Native (the judge's reading of `seg-native-dragmid-light-full.png`): on the arc rows 603–605 at x_arc + .5 there is NO lightening (229–237,
below the interior's 240); rows 608–612 show only the R channel (+4 … +15 with G −2 … −9 = the dispersion's R lane, not KeyFill); the
corner outside the capsule (row 602 x 108–123) is the page. So the main band (α .93, e ∈ [0, 1]) runs only along the two straight edges,
602–603 and 645–646, |x − 220| ≤ 88 (= x ∈ [R, W − R] in lens-box coordinates); on the arcs only the diffuse tail (+4 … +8 two pt further in,
α .265 → V) and the dark line remain. `keyfill-highlight.md` §0a's second item ("the thin light line along the arcs") is withdrawn by that
reading. Recorded, not resolved: the shader's angular term (§2, ang = sat((n·dir − .174)/.826)) would give .65 of the band at 45° on the arc —
the reading says 0; the difference is the #36 layer's shape / clip (cornerRadii 0×4, seg §1b), for the data session.

| file:line | old | new | source |
|---|---|---|---|
| view.js:1354–1356 `mkHl(cls, band, colour, mult)`, band `"m"` | for each ring with `r.main`: `ringRect("hl", r.e0, r.e1, { stroke: url(#gid), "stroke-opacity": mult·a })` — a full rounded-rect ring under the angular gradient | two `<line class="hl" data-e0 data-e1 data-run="top"/"bot">` per ring, `stroke: colour` (no gradient — ang = 1 on the straight runs: n·dir = 1), `stroke-opacity: mult·a`, `stroke-linecap: butt`; the `hgrad` for `"m"` is no longer referenced | predict §6.1 rows 5–6 |
| view.js:1389–1390 `rimGeo` | rects only | also `for (const l of s.querySelectorAll("line[data-e0]")) { const e0 = +l.dataset.e0, e1 = +l.dataset.e1, em = (e0 + e1) / 2, y = l.dataset.run === "top" ? em : Hd − em; l.setAttribute("x1", R); l.setAttribute("x2", Wd − R); l.setAttribute("y1", y); l.setAttribute("y2", y); l.setAttribute("stroke-width", e1 − e0); }` (R = Hd/2: the runs end where the arcs start, x ∈ [R, W − R] = |x − 220| ≤ 88 on the 220×44 lens) | same |
| view.js:1376–1379 `?segx=wide1` | merges `rect[data-e0]` runs | also `line[data-e0]` (or leave the experiment to rects — it is an experiment) | — |
| index.html:596–612 (comment ①) | "Angular fall-off along the arcs as the strokes' gradients: main sat((cos φ − .174)/.826) (gone at 80° from the top → no light line at the ends, §0a)" | "main band: the two straight runs only (predict §6.1 rows 5–6 — the native arc rows 603–605 carry no lightening; §0a item 2 withdrawn); diffuse: full rings with the first ring's angular ratio" | predict §6.1 |

Expected after: arc rows 603–605 at x_arc + .5 = the backdrop's own value (no V lift; native 229–237); x_arc + 1.5 rows 603–605 +4 … +8
two pt in (the diffuse tail); the straight-run rows 602 / 645 unchanged (V of the displaced backdrop: 247.5 / 250.0 native).

## 5 Not in this patch (unchanged, still right)
ring shadow `rect.rs` (§4: offset 8 / opacity .1 / stroke 4 / σ 3 / mask 0 — its −3.3 tail on row 646 is in the judge's −29.8); the #36 rings
(`hlRings`, three emits); the vibrant matrix stacks; the B5-d drift (`scaleSpring`) — the ui session's own item.
