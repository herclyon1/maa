# The phone page against Apple's Human Interface Guidelines

Two sources, both from Apple, both read 2026-09-14:

1. **The HIG** - https://developer.apple.com/design/human-interface-guidelines,
   the live site, read through its JSON endpoints (lists-and-tables, toggles,
   tab-bars, segmented-controls, materials, layout, typography, color, feedback,
   settings, designing-for-ios, motion) - the edition that describes Liquid
   Glass, i.e. iOS 26 and later. It gives the rules, mostly not the numbers.
2. **The iOS and iPadOS 27 UI kit** (Apple's Figma design kit, the user's copy,
   file key in `~/.config/ark/push.env` as `FIGMA_IOS_KIT`), read through the
   Figma REST API (`/v1/files/<key>/nodes?ids=<node>`). It gives the numbers.
   The node ids in the table are the components the value was read from:
   Row `550:50430`, Section Title `50:56535`, Grouped Table Footer `35:55757`,
   Separator `5469:7924`, Toggles `5433:19059`, Tab Bar `3:70967`,
   Tab Bar Button `5735:65307`, Row-Button `550:49677`, Text styles `5418:17464`.

3. **The simulator itself** - **Simplified Chinese** (so Settings uses the same
   PingFang metrics as the page). The numbers below were measured on iPhone 17 Pro
   Max / iOS 26.5, Settings › 辅助功能 and › 动态效果, pixel by pixel on 2026-09-15
   00:13; that runtime was deleted the same morning and the device is now iPhone
   18 Pro Max / iOS 27.0 (`8E793B8A-922B-46BC-86E2-E0F2BE845CA5`) - re-measure
   there before trusting a number that iOS 27 could have changed. Where the kit and the
   simulator disagree, the simulator wins: card margin 20.33, corner 26-27, row
   53.33, text inset 20 (kit: 16), separator hairline inset 20 on both sides
   (kit: left only), toggle 63×28 inset 18, header 17 semibold 28 above / 6 below,
   footer 13/18 with 6 above / 24 below, cards 35 apart when nothing is between
   them, large title ink at the 20 margin. WebKit's own `switch` control renders
   71×31, so it is scaled by .887 to the Settings size.

## Acceptance numbers (2026-09-18: the page's values are `web/tokens.css`)

Since the 状态 tab rebuild (branch `ui`, step 2) every size and colour on the page is a
`--ios-*` variable from `web/tokens.css` (a copy of `~/Money/styl-work/remote-ref/tokens.css`,
written by the data session). Each variable's source is the comment line above it in that
file: **UIProbe** = the iOS 27.0 simulator's own UIKit read in-process (`UIProbe`,
spec-extract.md ④/⑤), **AX-n** = the accessibility frame dump of reference screen n
(`remote-ref/<n>-ax.json`), **NUMBERS n** = line n of the iOS 27 UI kit's number table,
**SAMPLED** = no original value, pixel-fit (marked red wherever used; none left after deep rounds ⑥/⑦). `web/accept.js`
checks the rendered DOM against the numbers below (hard-coded there on purpose: if it read
tokens.css a wrong token would pass); colours are compared as parsed rgb/rgba, not strings.
The 2026-09-15 table further down is kept as history; where the two disagree, this one wins.

| Item | Value (light / dark) | Variable | Source |
|---|---|---|---|
| root font, body | 17 / line 20.33 | `--ios-body-size` `--ios-body-lh` | UIProbe preferredFont Body |
| subheadline, footnote, caption 2 | 15/18 · 13/15.67 · 11/13.13 | `--ios-sub-*` `--ios-footnote-*` `--ios-caption2-*` | UIProbe (老清单 caption 11/13.33 was wrong) |
| large title | 34 / 40.57, bold | `--ios-large-title-*` | UIProbe; weight NUMBERS 56 |
| tint / link / red / green | (0,136,255)·(0,145,255) / (0,122,255)·(9,132,255) / (255,56,60)·(255,66,69) / (52,199,89)·(48,209,88) | `--ios-tint` `--ios-link` `--ios-red` `--ios-green` | UIProbe colour table (老清单 #0a84ff / #ff453a were iOS 26 values) |
| orange | (255,141,40) / (255,146,48) | `--ios-orange` | UIProbe (not #ff9500) |
| grouped bg / card / label / secondary label | (242,242,247)·(0,0,0) / white·(28,28,30) / black·white / (60,60,67,.6)·(235,235,245,.6) | `--ios-grouped-bg` `--ios-card-bg` `--ios-label` `--ios-secondary-label` | UIProbe |
| separator | 1.0 pt, (60,60,67,.12) / (84,84,88,.5), inset 20 / 20 | `--ios-separator-h` `--ios-separator` `--ios-separator-inset-*` | UIProbe (1.0, **not** 1/3 px as the old list said) |
| card | inset 20, radius 26, groups 17.67 apart | `--ios-card-inset` `--ios-card-radius` `--ios-group-gap` | AX-45 / UIProbe |
| row | 53.33 high, text x 20 | `--ios-row-h` `--ios-row-text-x` | UIProbe (not 52) |
| header | container 45.33, text top 18.67 → padding 18.67 / 6.33; 17 semibold secondary | `--ios-header-h` `--ios-header-text-top` | AX-45 |
| footer | container 30.33, text top 7.67 → padding 7.67 / 24.66 (+ group gap); 13/15.67 | `--ios-footer-h` `--ios-footer-text-top` | AX-45 |
| switch | 63×28 r14, inset 18, knob 37×24 pad 2, travel 22 | `--ios-switch-*` `--ios-switch-travel` (derived) | UIProbe (knob 37, not 38; inset 18) |
| switch motion | knob 0.35 s critical spring ω21 (`linear()`), no stretch; track cross-fade 0.2 s default curve | `--ios-motion-switch-knob-*` `--ios-motion-switch-track-*` | UIProbe CAAnimation trace (the recorded 0.34 s stretch keyframes are gone) |
| segmented control | 32 r16, lens 28 r14 pad 2, label 13 (selected Medium 500); lens 0.52 s ω17.5 ζ0.9; press dims to .2 in 0.47 s, back 0.2 s | `--ios-segment-*` `--ios-motion-lens-*` `--ios-motion-segment-*` | UIProbe / reference 34 |
| alert | 320 r34, pad-x 30, title top 22 (17 semibold), message gap 7.33 (15 secondary), buttons 48 r24 inset 16 gap 8; appear scale 1.2→1 + fade 0.40 s (ω22.86), dismiss fade only; dimming (0,0,0,.2) light / .48 dark | `--ios-alert-*` `--ios-motion-alert-*` `--ios-dimming` | UIProbe (deep round added the dark dimming) |
| alert glass (also menu, toast) | the END-TO-END measured transfer (spec-extract ⑨, flat backdrops, residual ≤ 2/255 light): `backdrop-filter: blur(20px) saturate(1.94) brightness(1.071)` + white overlay α .538 (dark: 1.73 / .755 / .135); blur 20 = glassBackground inputBlurRadius 5 ÷ the CABackdropLayer scale .25; dimming .2 / .48. The glass is a `.pane` sibling under the content, not the dialog itself, so the buttons' blend can see it | `--ios-alert-glass-blur/-saturate/-brightness/-white/-filter` `--ios-alert-backdrop-scale` `--ios-alert-dimming` (page alias `--alert-fill`) | UIProbe ⑨ (tools/touch/glass_bands.sh, glass_table.py, glass_validate.py); the earlier blur 5 + lift .147 was the layer's own matrix only |
| alert action fill | pane − 30.9/255 (offset −0.121, light) / + 27.9/255 (+0.109, dark), cancel and destructive identical → a `::before` under the label with `mix-blend-mode: plus-darker` rgb(224) (dark plus-lighter rgb(28)); Chrome constant α .131 / .127 (= offset ÷ this site's pane 236 / 220). The destructive button's own background is transparent (the list-row `.acts button.danger{background:var(--card)}` used to paint it white) | `--ios-alert-button-overlay/-fallback/-offset` (`--alert-button-blend`) | alert-pipeline-plan.md §1.8 (button_fill.py over 8 flat backdrops; ⑨'s −37 / rgb(218) had the sampling box over the red label) |
| alert text | title Semibold 17 and 取消 Medium 17 = labelColor (vibrancy matrix e −1 / +.95 clamps to pure black / white), left-aligned, frames (30,22,260,20.33) / (59,122,54) ; message Regular 15 left-aligned at (30,49.67,260,38) = pane − 0.4 (dark + 0.3): `plus-darker` rgb(153) / `plus-lighter` rgb(77) on the message block, Chrome constant 134 / 111; message bottom padding 20.33 (buttons top 108 − 49.67 − 38); destructive label = M_red × button fill: (235,42,45) light (measured on white), (255,105,108) dark (formula with pane ≈ 35) | `--ios-label` `--alert-msg-color/-blend/-fallback` `--alert-destructive` | spec-table ④ frames, AX-48d (title box x 90 = 60 + 30, width 260), alert-pipeline-plan §1.9 |
| alert position | standalone full-screen native frame (60, 406, 320, 172): centred horizontally, vertical centre 14 below the screen centre (406 + 86 − 478); `top: calc(50% + 14px); translate: 0 −50%` (translate, so the appear animation's transform scale does not fight it); the pane has no stroke and no shadow (glassBackground inputShadowAmount 0, no shadow layer in the chain) | — | spec-table ④; alert-pipeline-plan #10 |
| 'no visible difference' pass, page 1–2 (2026-09-18) | alert pane 60 pt larger than the frame with the dialog clipping to r34 (CABackdropLayer marginWidth 60.2, paramdiff 51b874b); action press = fill layer opacity 1 → .4 in .1 s (⑤ `_UIAlertControllerFilledBackground`), not an ink wash; default action = same fill, systemBlue label (no filled capsule - 48d 移除小组件 alert); the message blend applies only to the plain alert text, the confirm dialog's diff list keeps its own colours; no focus ring on the auto-focused action (Safari drew one, native has none); standalone dark status bar `black` via the meta switched at load (default painted a light strip over the black page); read-only values sit on the row's right on one line (AX-46 value row) instead of stacking under the label; inline text fields are the same compact value box as numbers, right-aligned (NUMBERS 49) instead of a full-width grey bar; demo mode keeps the machine online | — | data session standalone shots `remote-ref/shots-51b874b/`, paramdiff-51b874b.md |
| multi-select / icon single-select | the pill row and the icon grid are gone: a Settings navigation value row 「已选 N/M ›」 (value secondaryLabel, disclosure chevron 10.33×14 tertiaryLabel inset 20, long values ellipsised) opens a check-list sheet built on AX-38 (设置 › 添加新键盘 › 简体中文): sheet from y 62 with top corners 38 on the grouped background, nav bar 54 at y 82 with the 44 glass back button at x 20 and the 36 tint 完成 button at (380, 86), list starts 17.67 under the nav, rows 53.33 with text at x 40 and a 19×17.33 tint checkmark whose right edge sits 22.5 in from the row's right; tap toggles (multi) / moves (single, AX-37b) the check, 完成 writes the edit into the pending list, back discards; at least one option must stay (MaaEnd ends the task otherwise). Icon options show the two set icons at 28 (--ios-row2-icon) | `--ios-chevron-w/-h/-inset` `--ios-value-gap` `--ios-status-h` `--ios-sheet-radius-top` `--ios-nav-h` `--ios-modal-nav-top` `--ios-nav-button/-side` `--ios-modal-button` `--ios-group-gap` `--ios-row-h` `--ios-tint` | AX-38 (38-ax.json frames), AX-37b, AX-45 chevron; user decision 2026-09-18 |
| alert position (standalone) | `top: calc(50dvh + 14px)` puts the centre at 492 only when the web view covers the whole 956: with `apple-mobile-web-app-status-bar-style` `default` or `black` the standalone web view starts at y 62 and is 894 high, dvh follows it and the centre lands on 522 (data session 2285c01 and fd61e71: the earlier 1018 explanation was wrong). The meta is therefore `black-translucent` (Safari Web Content Guide › Configuring Web Applications: the only value that lays the web view under the status bar), `env(safe-area-inset-top)` = 62 there, the page paints the status-bar area itself (body padding-top + background, light/dark with the scheme), and the sheet's top edge is `env(safe-area-inset-top)` (= 62 = `--ios-status-h` in the clip, 0 in a browser). The style is fixed when the clip is added, so a page-side meta switch does nothing (fd61e71) and the dark-mode script is gone. Status-bar text colour under `black-translucent`: the system draws it and adapts — black on the light page, white on the dark one (data session standalone shots of 862de97) | — | shots-2285c01, fd61e71 |
| ghost click after an alert action | the page acts on the pointer up (touchUpInside) and the browser still fires its own click for that up; once the action has closed the dialog that click lands on whatever is under the finger — a tile below opened a second dialog (data session's standalone shots of 862de97). installPressables swallows the one click that follows any press it handled, wherever it lands (`ghost` flag, cleared next task); accept.js replays it on a synthetic tile | — | shots-862de97 |
| sheet colours = elevated level | a presented sheet is at UIUserInterfaceLevel.elevated: dark systemGroupedBackground (28,28,30) and secondarySystemGroupedBackground (44,44,46) instead of the base (0,0,0) / (28,28,30); light identical to base (data session probe `levels`, tools/uiprobe/uiprobe-colorlevels.json). `.sheet` redefines `--bg` / `--card` to `--ios-grouped-bg-elevated` / `--ios-card-bg-elevated` | `--ios-grouped-bg-elevated` `--ios-card-bg-elevated` | probe levels |
| first launch from the icon sinks 62 pt | after Add to Home Screen the FIRST launch of the clip starts with scrollY −62 (visualViewport offsetTop −62; env() and body padding were both 62 in the same shot), every time a clip is added; a relaunch starts at 0. view.js scrolls a negative offset back to 0 on load / visualViewport resize / 300 ms / 1200 ms (data session `?diag=1` shots of 9c22044) | — | shots-9c22044 |
| `?diag=1` | a fixed one-line readout (innerHeight, visualViewport, env(safe-area-inset-top), body padding-top, h1 top, scrollY, display-mode) for standalone shots, where there is no console — added for the first-launch sink the data session saw on 862de97 (title top 143 on the first launch from the icon, 92 after relaunch) | — | shots-862de97 |
| segmented control placement | only on the 状态 tab: layoutTabs / selectTab hide `#app > .segctl` on any other tab; the other tabs keep following `curQueue` | — | acceptance 2026-09-18 |
| other glass (tab-bar platter, nav-bar round buttons, nav bar on scroll) | layer matrix ≈ saturate 1.2 + white .147, blur = inputBlurRadius ÷ backdrop scale: platter 5 ÷ .5 = 10 px, nav bar 2 ÷ .5 = 4 px; SDF highlight .5 → .5px white ring; diffuse 8 / .15 → soft shadow | `--ios-glass-blur/-saturate/-lift` `--ios-glass-highlight-amount` (`--glass-filter/-fill/-rim/-diffuse`, `--nav-blur`) | UIProbe deep + ⑨ scale; not measured end-to-end yet |
| selection lens (tab bar `.glide`) | gaussianBlur inputRadius 2 + the read 4×5 colorMatrix (gain·x + e), written into the filter chain itself: `backdrop-filter: blur(2px) saturate(1.062) brightness(.807) contrast(1.4)` (dark 1.379 / .763 / 1.14; contrast = 1 − 2e, brightness = gain ÷ contrast, ≤ .14/255 off the matrix over 20 000 colours). No overlay: WebKit composites a backdrop-filter layer separately, so a `plus-darker` ::after painted opaque (simulator Safari). Platter, lens and buttons are siblings - a lens nested in the backdrop-filtered platter only filters the platter's own fill (Chrome 251 vs the matrix's 236) | `--ios-lens-filter` (`--ios-lens-brightness-css/-contrast-css`; `--ios-lens-saturate/-brightness/-matrix` keep the decomposition) | UIProbe deep `_UITabSelectionView`; verified by pixel in headless Chrome (lens 236 on a 254 platter) and the data session's Safari sampling |
| segmented selected segment (`.segctl .lens`) | a flat platter, not glass: `_UILiquidLensView.restingBackground` = `_controlForegroundColor` white (dark (235,235,245,.3)), per-corner radius 14, no shadow, no filter (the glass appears only while dragging); track tertiarySystemFill .12/.24; labels labelColor, MediumG3 13 selected / RegularG3 13 | `--ios-segment-selected-bg/-radius/-shadow/-filter` `--ios-segment-track` `--ios-segment-label-color` | UIProbe lensdiff/subtree (uiprobe-lensdiff-seg-*.json) |
| lens springs | switch knob UIViewSpringAnimationBehavior dampingRatio 1 / response 0.3 s (ω 20.94, sampled to 0.35 s linear()); segmented / tab-bar selection dampingRatio 0.85 / response 0.4 s (ω 15.71, sampled to 0.55 s linear()) | `--ios-motion-switch-knob-response/-damping-ratio` `--ios-motion-lens-response/-damping-ratio` + the `-duration/-easing` curves | UIProbe deep (`_transitionKnobToPressed`, `_updateSelectionToSegment`, `setSelectedItem`) - the earlier trace fits (ω21 / ω17.5 ζ0.9) were stand-ins |
| touch behaviour (all controls) | implemented rule by rule from `remote-ref/interaction-spec.md` (iOS 27.0, injected event recordings, 2026-09-18), durations and slops from `--ios-touch-*`: **segmented** - touch-down on an unselected segment changes nothing but dims its label to .2 over .43 s; a hold never selects; the up commits index + `change` + content in the same tick and sends the lens along the measured per-frame path (x as a `linear()` easing over 1.209 s with the 7.5 % overshoot at 476 ms, width/height as keyframes: ×1.24 at 192 ms, ×.925 at 492 ms; ×1.371 at 125 ms, ×1.136 at 476 ms) from `--ios-touch-segment-lens-x/w/h-keys`; touch-down on the selected segment lifts the lens to 220×44 after 100 ms and a drag moves it (target = segment under the finger's x, index unchanged until the up); releasing on the original = no event; releasing > 70 pt outside = cancel; every rapid tap counts (no debounce, mid-flight direction change), the same segment twice = one event; **tab bar** - touch-down on an unselected tab starts the lens gliding to it (lifted 119×64) after 140 ms, the up selects with the content switching synchronously, no distance cancel (250 pt away still selects), the selected tab lifts only; **switch** - the knob lifts 195 ms after touch-down (60.5×40 → 58×38), follows a drag 7 pt past the ends, flips at the up whatever the position/direction, the one exception being dragged beyond the far end (> 22 pt) and back; **buttons** - highlighted at touch-down, 70 pt slop (touchDragExit/Enter), trigger at the up inside; **alert actions** - no slop, highlight transfers to the neighbour under the finger. Controls set `touch-action: none` so the browser never converts the touch into a scroll (the Redmi bug) | `--ios-touch-inside-slop` `--ios-touch-alert-slop` `--ios-touch-segment-dim-*` `--ios-touch-segment-lift-*` `--ios-touch-segment-commit-delay` `--ios-touch-segment-follow-lag` `--ios-touch-segment-lens-x/w/h-keys` `--ios-touch-tab-glide-delay` `--ios-touch-tab-lift-*` `--ios-touch-tab-selected-lift-delay/-done` `--ios-touch-tab-drag-lift-w/-h` `--ios-touch-switch-lift-*` `--ios-touch-switch-flip-delay` `--ios-touch-debounce` (page aliases: the keys rendered as `--seg-x-easing` / `@keyframes lens-stretch`, lift scale ratios) | interaction-spec.md §0–§4 (G/T/L/U/A series); accept.js replays each rule as a synthetic PointerEvent sequence with timing assertions (44 items, incl. the lens path samples at 192 / 476 ms and a re-render guard) |
| half sheet (not used on this page yet) | corners top 38 / bottom 54 (display corner 62 − 8), content scale 0.9636; glassBackground blur 4 + white layer +0.48 (dark +0.12); grabber gaussianBlur 30 with offset −0.228 (dark +0.275) | `--ios-sheet-radius-top/-bottom` `--ios-sheet-content-scale` `--ios-sheet-blur/-white` `--ios-grabber-*` | UIProbe deep `UIDropShadowView._preferredCornerRadii`; the 38 curve fit is superseded |
| pull-down menu | 250 wide, pad 10, item 42 at x 16 | `--ios-menu-*` | NUMBERS 96 (corner 26 still 待 tokens) |
| buttons / rows press | press instant; release 0.25 s (button) / 0.5 s (row) ease-in-out | `--ios-motion-button-release-duration` `--ios-motion-row-release-duration` `--ios-motion-ease-in-out` | UIProbe |
| spinner | 0.8 s period | `--ios-motion-spinner-period` | UIProbe |
| tab bar | capsule 62 with 4 inset, 21 above the screen bottom (standalone); buttons 94×54 r27 (shrink evenly when five do not fit 440), label 10 semibold at capsule top + 39, symbol box 28; unselected label `--ios-tab-label`, tint when selected; lens slide 0.55 s | `--ios-tab-capsule-h/-bottom` `--ios-tab-button-w/-h/-pad` `--ios-tab-label-top` `--ios-tab-symbol-box` `--ios-tab-lens-radius` `--ios-tab-label` `--ios-motion-lens-*` | UIProbe UITabBar (platter 274×62 at y 873; 83 is the whole frame); platter = glass recipe, selection = lens recipe (rows below) |
| nav bar / edit bar | bar 54 under the safe area; round buttons 44 at 20 from the edge, 5 above and below | `--ios-nav-h` `--ios-nav-button` `--ios-nav-side` | AX-45 NavigationBar (0,62,440,54), BackButton 44×44 at x 20 |
| tiles (actions and stamina) | 196×80.33 two columns, gap 8, corner 16 continuous; badge 48 at (6,2); number SF Rounded Bold 28/33.67 at top 9, right inset 10; label SF Rounded Semibold 17/20.33 at (12,52); a tile with our 回满 footnote grows past 80.33 (Reminders has no third line) | `--ios-tile-w/-h/-gap/-radius` `--ios-tile-icon/-icon-x/-icon-y` `--ios-tile-number-size/-lh/-top/-inset/-font/-weight` `--ios-tile-label-size/-lh/-x/-top/-weight` | AX-57 geometry + deep round ⑦ (libinjectdump in com.apple.reminders: RemindersUICore FillView, TTRListBadgeView, UILabel fonts); card stays white with the resource's coloured circle (09-15 decision) |
| value box (刷到几点) | tertiary fill, radius 8, padding 6/11 | `--ios-value-box-radius/-pad-y/-pad-x` `--ios-tertiary-fill` | NUMBERS 49 |
| device row status dot | 11 diameter; on `--ios-green`, off/unknown `--ios-tertiary-label`; gap 8 to the text | `--ios-status-dot` `--ios-green` `--ios-tertiary-label` `--ios-value-gap` | AX-58 Messages list unread dot (UnreadIndicatorImageView 11×11); colours UIProbe |
| still without a token (kept at the old value, flagged in the CSS) | tile: resource image 36 inside the 48 badge, the /上限 suffix at .58em; tab button padding 6/8/7 (adds up to the probed label top 39), value box width 96; first header under the large title 10 (no source, data session: not set on purpose); menu corner 26 and its 0.22 s appear; toast corner 18 and its 0.2 s; `.notice` rise 0.28 s; the press fill 8 %; teal #30b0c7 of the 鸣潮 tile; the switch's pressed lens look (Kit Toggle Pressed, its .12 s in/out); unselected tab image treatment (opacity .7 / saturate .6) | — | 待 tokens / 无来源 |

Dynamic Type: the sizes are the probe's px at the default text size, so the page no longer
scales with Settings › 显示与亮度 › 文字大小 (the old `-apple-system-body` em scheme did).
Root font-size is set to 17 explicitly.

Running it: the ui worktree's page with `?accept=1&quiet=1` on the iOS 27.0 simulator
(`scripts/mac/phone-accept.py`). In `?accept` mode with no mailbox configured the page
renders a built-in demo snapshot (`DEMO` in view.js: two shifts, one pending edit, stamina,
receipts; the device row says 「演示数据」) so every block exists for the probe without
real credentials; 99/99 light and dark in headless Chrome on 2026-09-18, with and without
stored credentials (desktop Chromium has no safe area, so the top-bar line there measures 54 without it).
The split commit 407376d alone (before step 2) measures identically to `main` with the
2026-09-15 accept.js: 61 rows, same 55 pass in Chromium (the 6 failures are Chromium's
16 px `-apple-system-body`), light and dark.

## Acceptance (2026-09-15, after ~/Money/transit's accept.js)

`scripts/mac/phone-accept.py` opens the published page in the simulator's Safari with
`?accept=1&quiet=1`; `web/accept.js` (loaded only then) measures the DOM against the
numbers in this file - root 17, margin 20, corner 26, row 53.33, hairline 0.34, toggle
63×28 / 38×24 inset 18, header 17/600 28-6, footer 13 6-24, tiles 80 / 80.33 radius 16
gap 8, device card 72, tab bar 62 with 54×72 buttons, top bar 44 + safe area, large
title 34/700 - and stores the result in localStorage; the script reads it out of
Safari's database in the device directory (nothing leaves the Mac), runs light and
dark, exits 1 on any item off. 46/46 on 2026-09-15 09:45 both ways. Since 12:1x it also probes **behaviour** on synthetic controls, not only geometry: state colours (switch on/off, danger, tint), the pressed lens, and a tap through the page's pointer handling followed by the browser's own click - the switch must flip exactly once and fire `change` once (the 12:0x regression: a tap flipped twice and stayed put on his Android, and the geometry-only run said 59/59). **A change that
fails it is a regression: fix the CSS, or - with a new measurement - this table first,
then `accept.js`.** The other session's version (`~/Money/transit/ui/accept.js`,
`DESIGN-HIG.md`) POSTs to a local server instead; the two should merge into one shared
kit (proposed to that session 2026-09-15 09:38).

A rendering check runs in the iOS 27.0 simulator (Xcode 27.0, iPhone 18 Pro Max)
next to the Settings app; `scripts/mac/sf-symbols-export.swift` pulls the tab
symbols from the system font. Each rule below is the source's wording condensed,
followed by what `web/index.html` / `web/app.js` does about it. A change to the
page that breaks a line here is a regression.

| HIG page | Rule | On the page |
|---|---|---|
| Lists and tables; kit Section Title `50:56535`, Footer `35:55757`, Row `550:50430` | iOS grouped style: headers, footers and extra space separate groups. Kit: header 17pt semibold secondary label, 9pt below it; footer 13/18 secondary label, padding 8/16/6; row 52pt, 16pt side padding, title 17/22, subtitle 15/20 | `h2` 17px/600 `--dim` padding 28px 20px 6px; `.foot` 13px/18px padding 6px 20px 24px; `.row` min-height 53.33, padding 15.5px 18px 15.5px 20px, label 17/22, hint 15/20; hairline 1 device pixel inset 20 both sides; white inset card `.group` (26px radius, measured in Settings) |
| Lists and tables | Keep item text succinct; avoid over-large rows | one setting per row, hint under the name in 13pt; long option sets fold behind 「已选 N/M ›」 (progressive disclosure) |
| Toggles; kit `5433:19059` | A toggle chooses between two opposing values; make the states obvious, not by colour alone. Kit (iOS 26): track 64×28 radius 100, on #34c759, off `rgba(60,60,67,.3)`, knob a 38×24 white pill | every on/off setting (config and relay) is the same 63×28 `.sw` with a 38×24 pill knob. Its motion was measured on the simulator's own recording of Settings › 动态效果 › 首选非闪动光标 (2026-09-15 04:58, `simctl io recordVideo`, sampled at 120/s): the whole flip ≈0.34 s - the knob stretches and lands on the far side within ≈0.1 s, relaxes back to 38pt over the next ≈0.2 s, the track cross-fades green↔grey over ≈0.2 s. The page's `knob-on/knob-off` keyframes (0.34 s, stretch to 46 at 30 %) and the 0.2 s track transition reproduce that; recorded the same way, the page's flip lands and cross-fades within ≈50 ms of the native one; WebKit's own `switch` control was tried and dropped - 71×31 and no motion; knob position + green; nothing on/off is a button. Flipping one does not send anything: it joins the 待保存 list and goes out after 保存修改, like every config row; pressed = the Liquid Glass lens from the kit's Toggle - Switch State=Pressed (read in the Figma web UI 09-15 11:2x, no API): knob 58×38 overhanging the track 5 above/below, Glass Effect layer white 9 % + 0.5 inner stroke #ccc + inner shadows + Specular Light; the page drives the switch with pointer events (lens while held, knob follows a drag, snap on release) because a 0.9 s hold in Safari otherwise flips nothing; verified frame by frame on the simulator recording 11:35 |
| Tab bars | Navigation only, not actions; keep tabs visible; single-word labels; use the number of tabs the app needs but 「it's generally easier to navigate among fewer tabs」; avoid overflow (when the width runs out iOS turns the trailing tabs into a More tab) | five tabs 状态 · 方舟 · 终末地 · 鸣潮 · 手机, always visible, floating at the bottom. There is no hard cap of five for tab bars (that number is the HIG's guidance for *segmented controls* on iPhone); with more games than fit, the plan is a 游戏 tab holding a list with detail pages (which then also gets the back button and gesture) |
| Materials (Liquid Glass) | Controls and navigation float above content in Liquid Glass; do not use it in the content layer; use it sparingly | only the tab bar, the save bar and the toast are glass (translucent + blur + specular edge); cards are solid standard material |
| Layout | Order by importance, align, group related items, differentiate controls from content | 16pt margins, name left / control right on one baseline, hairline separators inset to the text, min 44pt row height |
| Typography; kit text styles `5418:17464` | Large Title 34/41, Body 17/22, Subheadline 15/20, Footnote 13/18, Caption 2 11/13; system font | `h1` 34/700, row names 17/22, values 17, hints 15/20, footers and receipts 13/18, tab labels 10/12 (kit tab button), `-apple-system` first |
| Color; kit Colors `5707:28659`, Row-Button `550:49677` | Use system colours; support Dark Mode. Kit: tint #0088ff, destructive #ff383c, disabled #aeaeb2, secondary label `rgba(60,60,67,.6)`, separator `rgba(60,60,67,.29)`, green #34c759 | `:root` tokens `--accent:#0088ff`, `--bad:#ff383c`, `--dim:rgba(60,60,67,.6)`, `--line:rgba(60,60,67,.29)`, `--ok:#34c759`, grouped background #f2f2f7 / black; dark values under `prefers-color-scheme` |
| Feedback | Integrate status feedback near the item; confirm significant actions; alerts only for critical, actionable information | every order shows 「已寄出 … 等回执」 under its own row and clears on the machine's receipt; a toast confirms sends; `confirm()` only before spending sanity/波片 or stopping everything |
| Settings | Minimise settings; respect systemwide settings, no redundant versions | the 外观 group (light/dark override, accent colour) was removed 2026-09-14; the page follows the system appearance |
| Designing for iOS | Limit onscreen controls; reachable controls in the middle/bottom | one page per tab; the tab bar and save bar at the bottom |

| Tab bars; kit Tab Bar `3:70967`, Tab Bar Button `5735:65307` | Consider SF Symbols for tab icons; include labels. Kit: glass capsule 62pt high, buttons 72×54 with padding 8/6/7, a 28pt symbol line, label 10pt semibold; selected = capsule #ededed (light) / #121212 (dark) with tint; unselected #1a1a1a / #f5f5f5 | `nav.tabs .seg` 62px capsule with 4px inset, buttons min-width 72 height 54 padding 6px 8px 7px, `.ico` 28×28, label 10px/600, `.glide` capsule `--tabsel`, `--tab` for unselected; 状态/手机 are SF Symbols exported from the system font by `scripts/mac/sf-symbols-export.swift` as CSS masks (`TAB_ICONS`: gauge.with.dots.needle.67percent, iphone); the three game tabs show the games' own icons (`TAB_IMAGES`, desaturated until selected) - his 09-15 order after a morning with shield/mountain/waves (「毫不相关」) |
| Motion | Add motion purposefully; brief and precise; avoid motion on frequent interactions; make it optional | the selection capsule slides in the tab bar (0.28s); tab content switches instantly (a cross-fade read as 「闪一下」 and was removed); switches and pressed rows use the system-like 0.2s; everything is off under `prefers-reduced-motion` |
| Buttons; kit Row-Button `550:49677` | Full-width text rows in a group; destructive in red. Kit: 52pt row, 17pt regular, text left-aligned, tint / #ff383c destructive / #aeaeb2 disabled | `.acts button` 52px, 17/22, `text-align:left`, `--accent`; `.danger` = `--bad`; disabled = #aeaeb2; stacked full-width rows with inset hairlines (no side-by-side grid - its middle divider never lined up); the 复制免输入链接 row is one of these, not a pill beside a label |

| 状态 tab blocks (2026-09-15) | see PHONE-NATIVE-REFERENCES.md | device card = Find My › Devices card structure (icon, name, status line) on the Settings card metrics; notice cards = Health › 摘要 (inset 16, caption 13 + hairline, title 17 semibold, body 15 secondary, tinted capsule button 30 high, cards 10 apart); action tiles = Reminders' 2×2 tile geometry (two columns, 8 gap, 80 high, radius 16) with Find My's tile look (28pt coloured circle + SF Symbol, 15 semibold title, 13 secondary subtitle); 明日安排 = Settings rows instead of a `<pre>`; 2026-09-15 afternoon: the 跳过下一趟 tile is gone - each queue is a row under 明日安排 (「早班 · 09:00」, hint 东京 time · 今天照常/今天跳过) with a switch that goes through the same 待保存 → 完成 → receipt path as every other relay switch (`skip_today` off / `unskip_today` on, snapshot `relay.今天跳过`); pending edits turn the nav bar into an edit bar instead of a bottom bar: ✕ (放弃) and ✓ (完成) as 44pt round glass buttons 20 from the screen edges, centred in the 44 bar under the safe area, ✓ tinted; title 「待保存 N 项」 centred - measured on 通讯录 › 新建联系人 and the 设置 back button, 2026-09-15 10:54 (the first version used text capsules from the kit; the sheet uses symbols); pickers: every `<select>` is hidden behind a value+chevrons button that opens an iOS pull-down menu (`.menu`, measured on the simulator's own long-press menu 09-15 09:20: 248 wide, 42 rows, 17 regular, 5 padding, corner 26, glass, checkmark in the leading 22pt slot) - the native popup and the tap focus ring were what his Android showed (10:32) |

| Number tiles (2026-09-15) | Reminders' 2×2 tiles | the page asks 森空岛 (明日方舟/终末地 sanity, requests signed in the browser - `web/stamina.js` ports `skland.sign_headers`) and 库街区 (鸣潮 waveplate) itself; both APIs allow cross-origin calls (preflights verified 2026-09-15). Only Skland's token→cred exchange cannot run in a browser, so the relay hands its session over in the snapshot (`密钥.sk`); the Kuro token/did are pasted on the 手机 tab. Read only on page open, pull to refresh and the 刷新 tile; a reading younger than a minute is reused; no timer; the tile is Reminders' own: white card 80.33 high radius 16, a 32pt coloured circle with a white symbol top-left, the count 24pt bold in the label colour top-right with the limit as a grey suffix, the name 15pt semibold grey at the bottom; the tile's icon is the resource's *own in-game icon* (`RES` in app.js: 明日方舟 理智 from arknights.wiki.gg Sanity.png, 终末地 理智 from endfield.wiki.gg Sanity.png, 鸣潮 波片 from 库街区's widget asset energy.png; fetched through the simulator's Safari 「保存到照片」 because both wikis sit behind Cloudflare and refuse curl) on the game's coloured circle, because the tab bar already carries each game's own icon - his 09-15 order: the tab shows the game, the tile shows the resource, the two must not repeat. The gradient-filled version of 09-15 morning was Shortcuts' tile, i.e. 「tap to run」, on a number that is only read - replaced. Live values: Skland's Arknights `ap.current` is frozen at `lastApAddTime` (the page showed 2/210 all morning) - the page adds 1 per 6 min up to `max`; Endfield's `curStamina` is live (7.2 min a point by `maxTs`); Kuro's `akiBox/baseData.energy` is frozen too - the page reads the widget cache `gamer/widget/game3/getData` (`energyData.cur/total/refreshTimeStamp`, CORS verified 09-15), asks `refresh` only when that cache is older than ten minutes and keeps the cache when refresh answers 「操作频繁」 (every page open hit that limit on 09-15 10:42); the live count is derived from `refreshTimeStamp` (1 per 6 min) so the cache is always current |
| App icons (2026-09-15) | Apple's Icon Composer (Xcode 26 › Open Developer Tool) | the home-screen icon is an Icon Composer document (`data/icon-source/remote.icon`: system blue gradient + the `gamecontroller.fill` symbol as a white Liquid Glass layer) exported with 「Export Icon as PNG」 at 1024, the transparent corners filled with the icon's own gradient so the file is a full-bleed square (iOS masks it itself); `apple-touch-icon.png` 180, `icon-192/512.png` |
| Typography (Dynamic Type) | `-apple-system-body` | the root font is the system Body text style and every size is in em of it, so the page follows Settings › 显示与亮度 › 文字大小 like a native list |
| Navigation bars | large title collapses into a compact bar on scroll | `.topbar` (glass, 44pt, 17 semibold centred) is driven by scroll position: its glass and hairline appear as content slides under it (`--bar`); measured on the simulator's recording of Settings (2026-09-15 05:04, slow drag) the large title fades out while sliding under the bar and the small title fades in only after it is gone - so `--big` fades the large title over ~90 % of its height and `--title` starts the small one at the 50 % mark (no cross-fade, no jump) |
| Refresh | pull to refresh with UIActivityIndicatorView | touch pull past 72pt at the top grows the eight-bar indicator; release spins it and calls `ping()`; the 刷新 tile shows the same indicator in place of its symbol while the machine is being asked |

Not done (and why): the swipe-back gesture belongs to a navigation stack
(pushed detail views); this page has tabs, not a stack, and the HIG tab bar has
no back gesture. If a detail page is ever added, it gets a real back button and
the gesture with it.
