# 00 - 系统总览

> 全部已落地组件的一张总账:什么东西、在哪台机器、怎么被触发。
> 更新于 2026-08-20。事实变化时更新此页,别让文档和现实脱节。

## 组件清单

### Mac(控制端,东京)

| 组件 | 触发方式 | 位置 | 状态 |
|---|---|---|---|
| Fleet Monitor.app | **事件驱动**(tailscaled 消息总线推送) | `~/Applications` · 源码 [scripts/mac/FleetMonitor](../scripts/mac/FleetMonitor/main.swift) | 常驻运行 |
| 会话瘦身.app | 手动双击 | `~/Applications` · 由 [make-app.sh](../scripts/mac/make-app.sh) 打包 | 可用 |
| 内存采样器 | LaunchAgent 每 600 秒 | `~/.local/bin/mem-sample.sh` · 源码 [scripts/mac/mem-sample.sh](../scripts/mac/mem-sample.sh) | 常驻运行 |
| edit-json.py / push.py | 手动工具 | [scripts/mac](../scripts/mac/) | 可用 |

> **Mac 不在任何自动链路上。** 中继代码与配置的更新走 GitHub:推送到仓库 → 游戏机开机时 selfupdate/inbox 自取自用。曾有一个 Mac 侧推送脚本(deploy-when-online),2026-08-18 裁撤——与 selfupdate 重复,且把另一国的笔记本放进了无人值守链路。
| WARP + Tailscale 共存 | 常驻 | WARP 连 HP(Include 模式)· Tailscale 连游戏机 | 已验证 |

### 游戏机(乌鲁木齐,Tailscale `100.65.39.119`,主机名 INS)

| 组件 | 触发方式 | 位置 | 状态 |
|---|---|---|---|
| AUTO-MAS + MAA + MaaEnd | 计划任务,每日 09:00 / 21:30(服务器时间) | 游戏机本体 | 生产运行 |
| ark-do / ark-shot | Mac 经 SSH 触发计划任务 | `C:\ProgramData\` | 生产运行 |
| 中继(Windows 服务 `ark-relay`) | **事件驱动**:目录变更通知 + AUTO-MAS 进程句柄 + 精确闹钟 | `C:\ProgramData\ark-relay\` | 生产运行 |
| 代码自更新(selfupdate)+ 配置收件(inbox) | 开机事件:service 启动即对照 GitHub manifest,有变更拉取并**自重启生效** | 同上,源头是本仓库 `relay/` | 生产运行 |
| 开机自启 + 完成关机 | 计划任务 | — | 生产运行 |

### HP 云服务器(大阪)

| 组件 | 触发方式 | 位置 | 状态 |
|---|---|---|---|
| cloudflared 隧道 `osaka` | 开机 VBS 静默自启 | `C:\Users\human\srv\` | 运行 |
| OpenSSH sshd(端口 2222) | 同上,sshd-loop.ps1 守护 | 同上 | 运行 |
| RustDesk(ID 196029299) | 同上 | 同上 | 运行 |
| 中继 server 模式 | — | — | **已裁撤(2026-08-20),被 GitHub 开机监督取代** |

### GitHub(7×24 云端,仓库 herclyon1/maa)

| 组件 | 触发方式 | 位置 | 状态 |
|---|---|---|---|
| 开机监督 watchdog | Actions 日程(cron 是日程,非轮询):服务器时间 09:20 / 11:15 / 21:45 / 23:10 各查一次 Tailscale `lastSeen`,该报到没报到 → Server酱 | [.github/workflows/watchdog.yml](../.github/workflows/watchdog.yml) + [scripts/watchdog.py](../scripts/watchdog.py),配置 [queue/watchdog.json](../queue/watchdog.json) | **待启用**:需在仓库 Secrets 里配 `TS_OAUTH_CLIENT_ID` / `TS_OAUTH_SECRET` / `SERVERCHAN_KEY`,再把 watchdog.json 的 `enabled` 改 true |
| 收件箱(配置指令) | 手机改 `queue/config.json`,机器开机自取 | [queue/](../queue/) | 生产运行 |
| 代码分发(selfupdate 源头) | 推送到 `relay/` + 重建 manifest + [purge-cdn.py](../scripts/mac/purge-cdn.py) 清 CDN | [relay/](../relay/) | 生产运行 |

> 为什么机器不上传、只下载:游戏机对 github.com / api.github.com **TCP 层不通**
> (2026-08-20 实测 0/6),写方向物理不存在。开机监督因此反过来做——机器什么都
> 不发,tailscaled 连上/断开本身就是「开机报到/收工报到」,GitHub Actions 去
> Tailscale 那里读。这正好落实 2026-08-18 的裁决(#12):不要周期心跳。

### 不入库的本地文件(公开仓库,不放敏感内容)

| 文件 | 为什么不入库 |
|---|---|
| `~/Claude/HANDOFF-hp-cloud-server.md` | 含 token / 密码引用 |
| `~/Library/Application Support/FleetMonitor/specs.json` | 含真名与硬件明细 |
| `relay/.env` | 密钥 |
| `~/Claude/maa-automation-backup/` | 会话转录与配置备份 |

## 定时与轮询清单(全量)

只记事实,不做裁定;每一项的去留由用户判断。

> 2026-08-18 深夜复核修正:首版清单把生产路径 service.py 误标为轮询——它早已是事件驱动
> (目录变更通知 + 进程句柄 + 精确闹钟);把 inbox 误标为"每轮询问"——它只在开机时取一次。
> 真正的轮询残留是 PWA 网页、`__main__` 里的 local/agent 开发循环、指令按节拍拉取,
> **三处已于当日全部铲除**,记录如下。

| # | 所在 | 旧机制 | 现机制 | 状态 |
|---|---|---|---|---|
| 1 | 中继 local/agent 循环([\_\_main\_\_.py](../relay/ark_relay/__main__.py)) | ≤300 秒扫 history 目录 | [watch.py](../relay/ark_relay/watch.py) 目录变更即醒(Windows ctypes / macOS kqueue,零依赖);失败自动回退兜底扫描 | ✅ 已去轮询 |
| 2 | 中继 service.py(生产路径) | — | 本来就是事件驱动:目录通知 + AUTO-MAS 进程句柄 + 精确闹钟,3600 秒保险丝 | 无需改 |
| 3 | 中继 PWA 网页(server.py) | `setInterval` → SSE | **随 server 模式整体删除(2026-08-20)**,状态查看改走微信推送 + GitHub 网页 | ✅ 已消灭 |
| 4 | 采集端指令收取 | 随主循环每轮问一次 → HTTP 长轮询 | **随 server 模式整体裁撤(2026-08-20)**:指令走收件箱(开机自取,事件) | ✅ 已消灭 |
| 5 | Fleet Monitor 主更新 | — | `watch-ipn-bus` 长连接推送 | 本就事件驱动 |
| 6 | Fleet Monitor 兜底 | — | 300 秒心跳,仅在订阅静默死亡时起作用 | 保险丝,保留 |
| 7 | 内存采样器 | — | 600 秒采样 | 用户豁免 |
| 8 | Mac 侧部署脚本 | 设计为周期轮询,从未启用 | **裁撤**:更新走 GitHub + 开机 selfupdate,Mac 退出链路 | ✅ 已消灭 |
| 9 | 游戏机计划任务 | — | schtasks 固定时刻/登录触发 | 日程,非轮询 |
| 10 | HP sshd-loop.ps1 | — | `-Wait` 阻塞等进程退出 + 2 秒重启退避 | 事件等待,保留 |
| 11 | 通知/自更新重试 | — | 失败后退避重试 | 重试退避,保留 |
| 12 | 采集端心跳 | 设计为每 300 秒一拍(从未实际运行) | 2026-08-18 用户裁决:**取消周期心跳**,改为开机报到 + 收工报到 + 到点核查。**2026-08-20 按此落地**:报到 = tailscaled 连接状态(机器零代码),核查 = GitHub Actions watchdog 日程 | ✅ 已实现(待配 secrets 启用) |
| 13 | service.py AUTO-MAS 活性(此前清单漏记) | 无句柄时每 120 秒 wmic 查存活 | WMI `Win32_ProcessStartTrace` 内核事件:python.exe 一启动立即挂句柄;拉起失败按翻倍退避重试(首次 180s,翻倍至 30min 封顶),连败 3 次告警。WMI 订阅失败才退回 120 秒检查(降级路径,该路径无退避,与旧行为一致,启动时记日志) | ✅ 已去轮询 |
| 14 | agent 模式心跳/扫描循环(`cmd_agent`) | 每 `ARK_POLL_SECONDS` 心跳+扫描 | **随 server 模式整体删除(2026-08-20)** | ✅ 已消灭 |

## 铁律

**① 轮询须经批准(2026-08-18 用户令)**
今后任何功能实现,一律不得默认采用轮询方案偷懒。确实找不到事件驱动路径、非轮询不可时,必须先向用户说明理由并获得明确同意;未经同意,任何轮询方案不得落地。存量轮询(上表)的去留同样由用户逐项裁定。

**② 别人机器上不留调试残骸(2026-08-18)**
在远程机器(尤其他人的电脑)上产生的调试文件、试验脚本、临时截图,当次干完必须清点删除。本机的调试产物一律放会话临时目录(`/private/tmp/claude-501/...`,重启即清),不污染持久目录。教训:一个叫 `mini` 的菜单栏试验品带着 `TEST` 标题在后台活了两天才被发现。
