# 待查

**开机后第一件事**看这里，处理完就从这里删掉。
长期的活在 [欠的活.md](欠的活.md)，不打算改的既定限制在 [OPERATIONS.md](OPERATIONS.md)。

## ~~晚班跑完没有自动关机（2026-09-04）~~ 已查明

**原因：我设了「这次别关机」，活干完忘了取消。** 中继日志：

```
09-03 21:46:32  ⏸ 有人按了「这次别关机」，本次关机已跳过
09-04 21:55:35  ⏸ 有人按了「这次别关机」，本次关机已跳过
```

这个开关吃掉**下一次**关机机会且**不带时效**。下午为了打活动关设上，
晚上 21:55 晚班跑完才被消费，机器白开一夜。连续两天都是这个。

排除的另外两条：
* 不是 `_last_round_manual`（"最近一轮是手动触发的"）——那条今天只在 12:56 触发过。
* 不是有东西自动按开关——桌面的 `中继关机开关.bat` 是纯交互菜单，
  没有任何计划任务或启动项调用它。它**直接写文件不经过中继，所以不留日志**，
  查"谁设的"时日志里没有设置记录是正常现象。

规矩记进 memory `skip-shutdown-must-be-cancelled`：收工前必查 `state/state.json`
的 `modes` 段——`skip_next_shutdown` 和 `debug_until` 两个键都不在，才算清干净。
（2026-09-08 状态收口之前这两样是 `skip-next-shutdown.flag` 和 `debug-until.txt`
两个独立文件，现在那两个文件不存在了，照旧文件去查会读成「都清干净了」。）

## 2026-09-07 开机后

- [x] **手动部署中继**（09-07 起已多次部署并回读哈希核对，okww_patches 的补丁在日志「启动：」那几行里都在位）
      ~~原文~~：**手动部署中继**（`ARK_HOST=100.65.39.119 scripts/mac/deploy-relay.sh`）：09-06 夜的拆分
      新增了 handle/missed/report/shutdown 和 okww_patches/，自更新不收新文件。
      放在 09:00 队列开跑之后做，别撞 08:45–09:00 的预更新。部署后看 relay.log 有没有
      「这一段出错」，并确认 `okww_patches` 的补丁贴上了（日志「启动：」那几行）。
- [x] 09:00 早班：基质刷取在清波寨 280→40 连刷三次，一次成功。预更新今早把 MaaEnd 升到 v2.28.0-beta.1、OK-WW 升到 v3.6.7-beta.2，没有假警报。
