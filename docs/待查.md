# 待查

开机后第一时间处理，处理完就从这里删掉。

## 晚班跑完没有自动关机（2026-09-04）

用户 09-04 晚上指出：**当天晚班队列刷完之后机器没有自动关机**。
（不是我手动关机那次的问题——那次是先失败后成功：`shutdown /s /t 60` 走计划任务
没生效，改用 ssh 直发 `shutdown /s /f /t 15` 才关掉，两个判据都确认了。
另外记一条教训：**ping 不通不能当作已关机**，Tailscale 的 ICMP 本来就不可靠，
判据要用 ssh 连不上 + tailscale status offline。）

**最可能的原因**（推断，未经日志确认）：`engine.py::_maybe_shutdown` 里有

```python
if self._last_round_manual(now, entries):
    note = "最近一轮是手动触发的，不当作当天收工，不关机"
```

当天下午到晚上我手动跑了十几趟 MAA（打月行水上活动关、fight 验证关卡），
每趟都会记成手动运行。晚班队列跑完时"最近一轮"很可能已经是手动的了。

**开机后怎么确认**：中继真走这条会把上面那行原话写进日志。

```bash
scripts/mac/winrun.sh --py scripts/mac/lib/arklog.py   # 或直接翻中继日志
# 找 "最近一轮是手动触发的，不当作当天收工，不关机"
```

* 日志里有那行 → 规则如设计生效，要讨论的是**这条规则本身合不合适**：
  自动队列已经跑完、人只是中间手动跑过几趟，该不该因此整晚不关机。
* 日志里没有那行 → 是别的原因，往上翻 `_maybe_shutdown` 的其它 guard
  （`_scripts_running` / `_pending` / `_deferred_update_busy` / 日报未发），
  或者看 tick 有没有在更早的步骤抛异常（`engine.tick()` 里前面一步炸了，
  后面的关机判定根本走不到——08-31 就这么栽过一次）。
