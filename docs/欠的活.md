# 欠用户的活

**为什么有这个文件**：2026-08-27 用户问「角色练度推荐怎么又欠我了？你记不住事吗？」
对话会被压缩，只留在脑子里的待办一定会掉。**答应的事当场写进来，做完当场划掉。**

## 待办

- [ ] **MaaEnd 早班卡「获得」弹窗**：调查结论（2026-08-27 下午）——
      上游用药流程里**有**处理该弹窗的节点 `AutoUseSpMedicationRewardsConfirm`
      （5 秒超时），#5267 里「缺模板、同 #4589」的论断是**错的**，维护者关闭有理。
      当天为何超时已无法证实：MaaEnd 下次启动会自动清空 debug 产物，
      早上的三张 on_error 截图已消失。按用户决定：**等明天早班看是否复现**；
      中继现已在 MaaEnd 失败当场抢救证据到 `state/evidence/<run_id>/`，
      复现时证据会留下。

- [ ] **`team/index`（攻略总列表）参数没摸清**，仍回「参数错误」。
      按角色查已经够用（`team/char-list?charId=`），总列表只是锦上添花。


- [ ] **中继 WMI 监听退出**：08-27 08:17 报过一次
      `进程启动事件监听退出，改用 120 秒活性检查`，要拿到具体异常再修。

- [ ] **AUTO-MAS 队列不推进**：MaaEnd 自己退出后队列停住，
      `Run.RunTimeLimit=40` 也没触发重试，`Game.CloseOnFinish=True` 也没关游戏。

## 已完成（保留一段时间，防止重复问）

- [x] 四个 OK-WW 补丁全部提上游（2026-08-27）：
      [#1625](https://github.com/ok-oldking/ok-wuthering-waves/pull/1625) 副本失败不拖垮日常、
      [#1629](https://github.com/ok-oldking/ok-wuthering-waves/pull/1629) 巢穴续刷 + 只刷指定点位、
      [#1631](https://github.com/ok-oldking/ok-wuthering-waves/pull/1631) 领奖顺序、
      [#1632](https://github.com/ok-oldking/ok-wuthering-waves/pull/1632) 主C饿死兜底。
      每个都带测试，且都在游戏机上用 OK-WW 自带的 Python 真跑过。
- [x] 限定 vs 常驻：官方接口**不给**这个属性（`labelType` 只是当期卡池标签）。
      常驻六星名单取自 cmyyx/cep 的 `src/data/banner.ts` `STANDARD_CHARS`：
      艾尔黛拉、余烬、黎风、别礼、骏卫。

- [x] 预更新四个程序的结果（08-27）
- [x] 鸣潮日常从唤醒到结束的完整流程 → [WUWA-DAILY.md](WUWA-DAILY.md)
- [x] 终末地角色练度 → Excel 四张表 + Server酱推送；已按「只要六星」重出
- [x] 角色练度规划：六星里 11 名未满 90，现有材料**全部够**，
      练完还剩 1637 万经验、400 万金币（不含突破材料）
- [x] MaaEnd 卡弹窗已提上游 → MaaEnd/MaaEnd#5267
- [x] 官方四档培养模版数值（基础60/6/60、晋级80/9/80、高阶90/9/90、完美90/12/90）
      与四档各自的资源缺口
- [x] 养成建议与配队攻略：`char-pair/char-list?charId=`、`team/char-list?charId=`
      —— 16 名六星全抓到，含推荐技能等级、推荐武器、推荐搭档与理由
- [x] 突破/技能/天赋的完整消耗规则：`calculate/rules?charIds=<单个id>`
      （逗号拼多个会返回空表，只能一个一个问）
- [x] 残象聚落 08-27 已刷（0/41 → 24/41），补丁三处 bug 全修
- [x] 结果核对 `outcome.py`：跑完按证据核对，没干成立刻通知，不再静默记账
- [x] MaaEnd 12:00 已真正开始跑（重启 AUTO-MAS 清掉了陈旧任务锁）
- [x] 闸门自检 `scripts/mac/guardcheck.sh`（15 条），接进部署闸门
- [x] 死代码检查 `scripts/mac/lib/deadcode.py`，接进仓库自检与部署闸门
- [x] 补丁改完立刻生效（中继一启动就贴，不再等第二天开机）
- [x] 只刷落渊南丘写进真正生效的母本配置 → [maa-config-master-copy 记忆]
- [x] 森空岛接口全部打通 → [SKLAND-API.md](SKLAND-API.md)
- [x] `winrun.sh --put`（修手拼 scp 用错用户名）
- [x] MaaEnd 更新通知带老版本号
- [x] OK-WW 启动的 error 740
