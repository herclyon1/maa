# 优化方案（2026-09-06 提出，等拍板）

**为什么有这份**：用户 09-06 要求「对程序、仓库、项目文件、工作流程整体优化一下，
按重要程度排序」。这里是扫完仓库之后的方案，**没有一条已经动手**。
判据只有一个：哪条最能减少「机器没干活 / 他不知道 / 我重复犯错」。

扫描时的仓库事实（都是工具量出来的，不是印象）：

| 项 | 数 |
|---|---|
| 提交 | 485（08-14 起，日均 20+） |
| 中继源码 | 14898 行 / 19 个模块；`engine.py` 1562 行，`_maybe_shutdown` 一个函数 130 行 8 道门 |
| 测试 | 61 个，全绿；自写跑器，lint 和部署各跑一遍 |
| 文档 | 36 篇 / 8029 行；**16 篇没有任何地方链接到它**；6 篇是英文写的（OPERATIONS 1383 行、PITFALLS 891 行、CONFIG、NOTIFICATIONS、HEADLESS、GAME） |
| 规矩存放处 | CLAUDE.md、TOOLING.md、PITFALLS.md、RETROSPECTIVE.md、memory（105 条，31 条「死命令」）、欠的活.md —— **六处** |
| 每次会话固定加载 | CLAUDE.md 7.4KB + MEMORY.md 15.2KB |
| 欠的活 | 15 条未勾，其中至少 3 条已经过期或已做完 |
| 开机监督 | `queue/watchdog.json` `enabled=false`，`pause_until=08-20`，Secrets 没配 |

---

## 第一档：不做就还会出事

### 1. 关机决策要出声，「别关机」开关要有时效
**问题**：`_maybe_shutdown` 的 8 道门任何一道说「不关」，只写 relay.log。
09-03、09-04 连续两晚机器白开一夜，原因是我没取消的 `skip-next-shutdown.flag`，
它**没有时效**，而且没有任何东西会告诉你「今晚不关机了，因为 X」。
本该兜底的 GitHub Actions 开机监督又是关着的。
**做法**：
- 到了本该关机的时刻却没关，**推一条**「今晚不关机：原因」（事件驱动，一天最多一条，不是轮询）。
- `skip-next-shutdown.flag` 写入时带过期时间：只对**当天下一次**关机有效，过了当天自动作废；开机时发现是隔夜的直接清掉。
- 把「别关机标记 / 调试模式 / 下一次关机机会」并进 `healthcheck.py`，收工体检一眼看到。
- 关机判定从 `engine.py` 拆成 `shutdown.py`：一个纯函数 `decide(now) -> (关不关, 原因)`，原因就是推送文案；`test_shutdown.py` 直接测它。
- 开机监督：你把三个 Secrets 配上，我把 `enabled` 改 true，`pause_until` 改成读调试模式。

### 2. 清理「欠的活」并给每条挂触发条件
**问题**：15 条未勾里，「09-04 方舟大版本维护日」已过去、「队列改名+通知样式」多半已做、
「1.5.3 适配」要看 MaaEnd 出没出新版。你问过「你记不住事吗」，账本本身过期就等于没记。
**做法**：现在逐条核实、该勾勾该删删；留下的每条必须写「**下次看它的条件**」
（某版本发布 / 某天早班跑完 / 你说一句话），没有条件的不许留。
上游 issue/PR（#5506、#5453、beta.6）做一个 `scripts/mac/upstream-watch.py`，
**我要看时**一条命令拉全状态，不定时跑。

### 3. 规矩只留一处，文档写不住的做成闸门
**问题**：同一条教训在 PITFALLS、RETROSPECTIVE、TOOLING、CLAUDE.md、memory 里重复两三遍，
每次会话光规矩就吃 22KB，而重复犯错并没有因此变少——你自己说过「写进文档里跟放屁一样」。
**做法**：
- 每条规矩三选一：**做成闸门**（lint-repo / guardcheck / 工具拒绝）→ 从文档里删；
  **一句话**放 CLAUDE.md；**背景故事**放唯一一篇中文《教训》，PITFALLS 和 RETROSPECTIVE 合并进去。
- memory 的 31 条「死命令」按主题合并到十条上下（例如「他说什么就做什么」「活没干完不许停」「不许说否定词」是一条），
  合并时保留原文出处。
- `check-docs.py` 接进 `lint-repo.sh`（现在没接），并加一条：每篇 docs 必须被 README 或 CLAUDE.md 索引到，否则报错——16 篇孤儿文档就是这么来的。

## 第二档：该做，但不做不会立刻出事

### 4. 文档全部改中文、大文件拆开、孤儿文档处置
- 6 篇英文文档改中文；`OPERATIONS.md` 拆成三篇（机器路径与时刻表 / 远程驱动 / 四个程序的更新机制）。
- 16 篇孤儿文档逐篇判：还有用的挂进索引，过期的删（例如 SWEEP-0830 是一次性的）。

### 5. 开工 / 收工做成脚本，不再临时拼
- `scripts/mac/start.sh`：机器时间、调试模式、别关机标记、预更新结果、今天队列——一屏。
- `scripts/mac/confirm-off.sh`：关机确认的两个判据（ssh 连不上 + tailscale rx 不涨）写死进去，
  我 09-05 靠 ping 误报过「已关机」，这类判定不该再手拼。
- 两者都进 CLAUDE.md 工具表。

### 6. 中继大模块拆分
`engine.py` 1562 行：关机（见第 1 条）、`_handle`、日报三块各自成模块；
`okww_patch.py` 1264 行按补丁拆文件。只搬不改逻辑，测试不动。

## 第三档：有空再做

### 7. 测试统一成 unittest，一次跑完
61 个测试各起一个 python，lint 13.8 秒里大半是它；部署时又跑一遍。
改成 `python -m unittest discover` 一次跑完，闸门判据不变。

### 8. `scripts/win` 并进 `scripts/windows`
两个目录放 Windows 脚本，没有区分理由。

### 9. `queue/config.json` 与 `inbox/todo.json` 二选一 —— **2026-09-08 做了**：删掉零引用的 `inbox/`，只留 `queue/config.json`；并把里面留着的旧 `debug_mode` 清空（版本号一抬就会被重放）
手机页已经是主入口，文件下发通道留一个就够。

---

**不在方案里的**：手机页 `app.js` 单文件 900 行（能用、改动频繁、拆了反而难改）；
`MedicineNumb=999`（你的设置）；HP 云服务器（已搁置）。

**排序理由**：第一档三条对应你说过最重的三句话——「怎么没关机」「你记不住事吗」「写进文档里跟放屁一样」。


---

## 2026-09-06 用户拍板与已做的事

用户的三条意见，按此重排：
1. 文档留英文——只有我读。第二档第 4 条里「改中文」那半句作废，拆大文件仍可做。
2. 「同一条教训记两三遍、错照样犯」——办法不是把规矩写得更多，是：判断类规矩改成
   **强制走工具路径**（报状态只能贴脚本输出、改配置必须打印回读 diff）；
   规矩条数压到一次拿得住；减少我动手的次数。
3. 真正的损失是**他的时间**：最近 6 天部署 95 次、14 天 128 条加功能 / 81 条修 bug，
   bug 是我每天新造的。用户否决「砍功能 / 冻结」，要求**直接把软件通读修好**。

当天做的（不加功能，全部有测试或闸门）：
* 通读中继全部 26 个模块 + service.py + 手机页。
* 修：`queues.apply` / `commands._run_now` 局部变量遮住 `names` 模块，09-02 起每次调用必崩
  （跳过队列、待办队列开关、手机「现在跑一趟」四天全废）。`test_queue_apply_runnow.py`。
* 修：`tick()` 一段出错带走日报和关机（09-04 事故）——逐段兜底。`test_tick_isolation.py`。
* 修：账本一行时间写坏 → 日报发不出 → 机器开一夜。`test_ledger_bad_times.py`。
* 改：`_scripts_running` 三秒缓存，一轮少起十来个 tasklist。
* 修：`GetTickCount64` 未声明 64 位返回；okww_patch 非法转义（3.14 会报警）；
  preupdate 与 check-docs 里的 PowerShell 5.1 回退删掉，只认 pwsh 7；
  check-docs 引用了从未定义的 `_has_pwsh`；scripts 里 8 处无用导入 / 假 f-string。
* 闸门：lint 第 9 项 = pyflakes 零报告 + 用机器的 Python 3.14 严格编译；
  lint 第 2 项正则补上列表写法；guardcheck 加两条坏样本。
* 部署：写好 RELEASE-NOTES，**机器已关机，等 09-06 08:40 开机后立即部署**。

## 屎山怎么改（2026-09-06 夜，用户问「那现在这个软件要怎么改」）

不砍功能、不冻结，在原地把它改成「改一处能知道影响哪里」的东西。顺序不能换：

1. **先立防线，再动结构**：真实一天的回放测试。把已经跑过的真实一天
   （AUTO-MAS history + 三个程序的日志 + 当天的推送和关机决定）存进 `relay/tests/replay/`，
   中继回放它，必须产出一模一样的推送文案和关机判定。以后每修一个 bug 就把那一天存进来。
   这是屎山唯一缺的东西：能证明「改了 A 没坏 B」。现在的 64 个测试是手编输入，证明不了这个。
2. **按缝切，不重写**：`engine.py` 拆成三块纯函数——记账（_handle）、报告（日报/临时查看）、
   关机判定（返回「关不关 + 原因」）；`service.py` 的 main 拆成开机阶段函数；
   `okww_patch.py` 一个补丁一个文件。每一步都只搬不改，回放测试全过才算搬完。
3. **注释里的事故史搬出代码**：代码只留一句「为什么这样写」，故事进 docs。
   现在一半行数是历史，读代码的人分不清哪句是规则哪句是往事。
4. ~~改动节奏：一天最多部署一次~~ **用户 09-06 否决：「这就是在偷懒，所有事故都由你偷懒引起，
   闸门全是为了防止你偷懒。设计一个彻底让你不能偷懒的东西。」** 改成：
   **偷懒不可能——四道机器执行的门，我一句话都插不进去：**
   a. **没有回放案例的改动不许部署。** 部署脚本读 git diff 里改了哪些模块，每个模块必须
      被至少一个回放案例真正执行到（回放时记录覆盖）；改判据的提交必须同时带一个新案例，
      否则部署直接拒绝。改了什么就得证明什么。
   b. **「部署完成」这四个字只能由脚本打印。** 部署脚本重启服务后自己在机器上跑冒烟：
      导入全部模块、按真实配置调一遍改过的函数（不派发、不烧体力）、读 relay.log 确认
      零「出错」行——全过才打「部署完成」，我不许在它没打之前说这四个字。
   c. **下一趟真实班次自动给这版打分。** 日报末尾固定一行：「代码 v…，这版跑过 N 趟，
      失败 M 趟」。这行由中继算，不经我手。我说「修好了」而它写「失败 1 趟」，谎话当场现形。
   d. **状态类结论只能贴脚本输出。** 「已关机」= `confirm-off.sh` 的输出；「已生效」=
      部署脚本的输出；「配置已改」= 回读 diff。做成三个脚本，输出带机器时间和判据原文；
      我要说这三件事，只能整段粘贴它们的输出。
5. **指标**：日报末尾「连续 N 天你没动过它」，和 4c 那一行放在一起。

用户对第 1 步的定性（09-06）：「你这个软件是外置的，相当于工头，只要 MAS 和它管的三个脚本
这四个程序的输出固定一次，中继每次跑的结果就该一致，能在 Mac 上实战演练。」——回放测试
的依据就是这句：输入是四个程序落盘的东西，输出是推送文案和关机判定，两头都能钉死。

工作量：第 1 步一个工作日，第 2 步一个工作日，第 3 步零散做。

## 2026-09-06 深夜：第二、三步已做（用户拍板「先做二三」）

第二步（提交 4592315）：
* engine.py 1595 行 → engine 477 / handle 512 / missed 145 / report 239 / shutdown 369。
  逐函数比对 49 个：47 个一字不差，`_archive_maaend_evidence` 只去掉一处局部 import，
  `_maybe_shutdown` 有意改成纯判定 `shutdown.decide()`（返回关不关 + 原因 + 哪道门）+ 副作用分开。
* service.py 的 main 675 行 → 10 个阶段函数，main 只剩顺序。
* okww_patch.py 1264 行 → okww_patches/ 一个补丁一个文件；25 个函数、全部常量逐个比对一字不差。
第三步：注释和 docstring 里带日期的事故经过共约 370 行搬到 `docs/CODE-HISTORY.md`，
代码里留第一句和「来龙去脉见 …」的指向；带「必须/不许」的规则行留在原处。
只做了第二步碰到的文件；其余模块下次按同一脚本再过一遍。

**未部署**：机器已关。拆分新增了文件，开机自更新不收新文件，明早开机后必须手动
`deploy-relay.sh`（放在 09:00 队列开跑之后，别撞预更新窗口）。

## 2026-09-07 深夜：根治五项，用户批准（「根治的那五项你都可以跑了，我批准了」）

当天量出来的画像：中继 1.57 万行、13 条 OK-WW 补丁、76 个测试、280 处带日期的
事故注释；14 天 388 次提交其中 113 次部署；当天 36 次提交 12 次部署。当天修的
bug 几乎全是「按当时看到的日志形状写的判断」失效：进程名单漏 OK-WW、旧版本号被
新进程覆盖、周本从未进步骤清单、失败原因靠一句日志分类、停服务事件类型错。

| # | 项 | 做什么 | 要不要开机 | 状态 |
|---|---|---|---|---|
| 1 | 判断输入换成上游结构化接口 | AUTO-MAS runtime-snapshot（已用）、MXU 12701 接口拿 MaaEnd 任务状态、OK-WW 的 info_set 状态、MAA 回调文件；日志/OCR 只做兜底 | 要（查接口形状、上机验证） | 待做 |
| 2 | 离线回放测试库 | 机器上的 history/日志脱敏存进 tests/replay，判断函数先过回放；新 bug 先加样本再修 | 要（拉样本） | 待做 |
| 3 | 状态收口 | 一个状态文件 + 字段表，取代十几个散文件；手机页/日报/关机判定都从它读 | 不要 | 待做 |
| 4 | 通知文案集中 | 所有通知文案进一个模块，禁英文/禁模糊/同类同句式由测试守 | 不要 | 待做 |
| 5 | 长函数拆分 | `_loop` 251 行、`parse_record` 140、`parse_okww_log` 140；preupdate 按程序拆 | 不要 | 待做 |

顺序：今晚 4 → 5 → 3（不用开机、都有测试守）；明早早班跑完的窗口做 1、2，
部署和上机验证也放在那个窗口，不碰早班。

### 2026-09-08 凌晨：五项的进度与第 1 项的接口清单

已做并上机（09-07 23:24、23:34 两次部署，机器上验证过）：
* 第 4 项 通知文案集中：`ark_relay/texts.py` + `tests/test_texts_gate.py`。
* 第 5 项 拆长函数：`preupdate` → `preupdate_common/_maaend/_maa/_automas/_okww`（AST 逐项比对 68/68）；
  `collector.parse_okww_log / parse_record` 拆成带名字的步骤。`service._loop`（251 行）留到白天窗口，
  拆完要看一整轮 tick 才算验证。
* 第 2 项 回放库：`tests/replay/` + `scripts/mac/pull-replay.sh` + `tests/test_replay.py`；
  已放进 2026-09-07 整天 10 条真实记录，判定人工核对过。
* 第 3 项 状态收口：`ark_relay/statestore.py`（单一 `state.json`、字段表、迁移）；周常三门和版本记账
  已搬进去（机器上迁入成功：annihilation/garden/weeklyboss/okww-version/maaend-version）。
  剩：当天标记与开关、更新流程、告警队列，按 docs/状态模型.md 的顺序。
* 顺手：`collector.scan` 原来每个周期把整个 history 几百条全部重新解析，现在见过的按路径直接跳过。

第 1 项（判断输入换成上游接口）——接口都查清了，实施要在程序跑着的窗口做：
| 程序 | 接口 | 状态 |
|---|---|---|
| AUTO-MAS | `GET /api/dispatch/runtime-snapshot`（每个脚本 完成/异常/运行/等待） | 已用于「有没有脚本在跑」 |
| MaaEnd（MXU） | `http://127.0.0.1:12701/api/maa/state`、`/api/ws`（事件流）、`/api/logs`、`/api/maa/instances/:id/*` | 只在 MXU 跑着时监听；早班窗口接 |
| OK-WW | 自带 fastapi：`/api/tasks`、`/api/logs`、`/api/events`(ws)、`/api/executor/*` | 只在 `gui.type == "web"` 时启动（config.py），要一条补丁改成 `type: web, launch_mode: server` 并固定端口；先在窗口里试一趟 |
| MAA | 结果 JSON 已由 AUTO-MAS 给；任务粒度看 `debug/asst.log` | 现状够用 |


## 2026-09-08 凌晨：五项全部处理完，逐项交代

| # | 项 | 结果 |
|---|---|---|
| 1 | 判断输入换成上游结构化接口 | **AUTO-MAS**：`/api/dispatch/runtime-snapshot` 已用于「有没有脚本在跑」。**OK-WW**：改用它自己写的 `info_set 键 值`（`current task`、`错误`、`Teleport to Tacet Suppression`、体力、日常进度），散文只做兜底；日报里的无音区说的是**实际刷了哪个**，和设置不一致会两个都说。**MaaEnd**：它的日志本来就是结构化标记（`任务开始/完成/失败`），已在用；MXU 的 12701 接口只在 MaaEnd 跑着时存在，要拿它得在运行期轮询，按「轮询须经批准」的规矩没做，留待用户点头 |
| 2 | 离线回放测试库 | `tests/replay/` + `scripts/mac/pull-replay.sh` + `tests/test_replay.py`；已放进 2026-09-07 整天 10 条真实记录（MAA 3 / MaaEnd 3 / OK-WW 4），判定人工核对过，改判据先过回放 |
| 3 | 状态收口 | `statestore.py`：单一 `state.json`、六段、字段表登记才能写、旧文件每次启动扫一遍迁入。机器上已迁 60 份旧文件；留作文件的只有账本、seen、证据、锁、心跳、纯缓存 |
| 4 | 通知文案集中 | `texts.py` + `tests/test_texts_gate.py`：标题不许写死在代码里，文案不许有英文、不许模糊 |
| 5 | 长函数拆分 | `preupdate` 拆成五个模块（AST 比对 68/68）、`collector` 两个 140 行函数拆成带名字的步骤、`_loop` 241 行拆成 `_DirWatch` + `_AutomasKeeper` + 短循环并补了单测。超过 80 行的函数从 16 个降到 13 个 |

**没做的那一件，和为什么**：MaaEnd 的 MXU 12701 接口。它只在 MaaEnd 进程活着时监听，
而中继是事件驱动的（文件变更 + 闹钟），要在一趟运行中拿到它的逐任务状态就得起一个
轮询。用户 2026-08-31 的死命令是「不许默认轮询，要用先问」，所以这条留着，等点头。
收益也有限：MaaEnd 自己的日志已经是结构化标记，我们本来就按它判。


## 2026-09-08：明确不做的一条

**注释中英混排不做统一。** 审查里有一条「注释和 docstring 中英混用没有边界，
同一个理由有 6 种写法，grep 一次只能命中一半」——统计属实：中继里中文注释 1121 行、
英文 747 行，29 个文件两种都有。

但用户 2026-09-06 已经拍过板：「文档留英文——只有我读，英文对我更准；不要提改中文」。
所以这一条按「不做」处理，只把分界写进 `CLAUDE.md`：用户会读到的字必须是中文
（通知、手机页、RELEASE-NOTES、欠的活、待查，`texts.py` 有闸门），
代码注释和 docs 的其余部分随写的人，**不许发起翻译运动**。
