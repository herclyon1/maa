## 2026-09-04 手机页大重构

- 终末地和鸣潮原来那 10 项设置**改了不生效**：这两个脚本的「快速配置」是关的，
  AUTO-MAS 根本不把那些字段下发给脚本，它们看的是自己那份母本配置。
  现在这两段直接改母本，按下去是真生效。
- 补上基质刷取的全套：使用刻写券、领取方式（不领取／单倍／双倍）、
  最大循环次数、地区选择（多选）、战后基质筛选、任务开关。
- 撤掉不用改的：关卡模式、连战、任务序号；剿灭和残象聚落点位改成只读显示。
- 字段名一律用脚本自己的译名。原来「模拟领域第几个／凝素领域第几个」是我编的，
  和官方译名正好编反了，现在按 OK-WW 自己的语言包显示。
