## 2026-09-02

- **手机页面不用再手动刷新了**：打开页面就知道机器在不在线，和 ToDesk
  一样。页面打开时告诉机器「我在看」，机器在这十分钟里每半分钟报一次
  平安；页面用一条长连接实时收，收到就翻「开机」，机器正常关机会先说
  一声「下线」，页面秒翻「关机」；硬断电靠一分半没消息翻过来。
  页面自己不轮询。机器只在有人看的时候报平安——ntfy 每天每台机器只给
  250 条消息，盲报会把额度吃光，连状态和指令应答都会被挡。
- **预更新通知统一**：四个程序都写成「程序 已更新：旧版 → 新版」。
  MaaEnd 的旧版本改从它自己的版本文件读（08-30 那次启动 10 秒就装完
  重启，日志里根本没来得及出现旧版本行）；MAA 补上前后版本号，待装的
  包从包名里读目标版本；AUTO-MAS 改成「有更新：旧 → 新（安装中）」；
  旧版本真读不到时明说「（旧版本没读到）」，不省也不编。
- **队列改名**：「新队列」→「早班」，「Evening-MAA」→「晚班」。
  中继认旧名：排队中的手机指令、跳过标记不受影响。AUTO-MAS 里的实际
  改名用新加的改名工具，改完回读核对。

