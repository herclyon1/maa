两件事，都不改行为。

一、三个最大的文件按职责切开了：日志解析按游戏分开、开机那十个步骤单独成篇、
客户端更新也按游戏分开。最大的文件从一千三百多行降到六百多行。
搬动前后逐个函数比对过，两百多个函数里连一个字节都没变——只是换了地方。

二、我在这台 Mac 上操作机器的速度提上来了：原来每敲一条命令要等八秒，现在两秒半。
原因是每次都在重新握手、而且取结果要单独再连一次。现在共用一条连接、
结果在同一次连接里带回来。这不影响机器上跑的东西，只影响我干活的快慢——
但你等我的时间就是这么攒出来的。
