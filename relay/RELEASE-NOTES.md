鸣潮残象聚落「只刷落渊南丘」从 09-10 起就没生效，四天每天早上把四个点位全刷了：OK-WW 加载设置时会把它不认识的那一项删掉并把文件重写，所以我们的改动跑起来时读不到点位名，就按原版刷全部。现在点位名直接从 AUTO-MAS 的母本读（中继开机写一个指路文件），日志里会写「只刷 ['落渊南丘']（设置来自母本）」。闸门补上两条：跑完的核对要求日志里有这句、而且这一趟只能进一个点位，进了别的就报「没干完」；收工体检和 okww_effective 也改成看上一趟的真实日志，不再只看母本。
日报里不是采集日的那趟终末地，不再写「做了 自动采集」；改成「自动采集 今天周X不是采集日（排班只有周一、周四），按排班跳过，没走路线」。
MAA 导出日志代码的核对基准更新到上游 09-12 的版本（只改了日志调用，打包方式没变）。
