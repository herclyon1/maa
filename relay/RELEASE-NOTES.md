把中继按功能拆开了，行为一个字没改：

· 记账/告警、漏跑检查、日报、关机判定各自一个文件；关机判定现在会先说清
  「关不关、为什么、卡在哪道门」，再去执行。
· 开机流程拆成十个步骤函数，顺序一目了然。
· OK-WW 的补丁一个补丁一个文件。
· 代码里堆着的事故经过搬到了文档，代码只留一句为什么。

拆完逐个函数比对过：除了关机判定那处有意的改写，其余一字不差；
既有的 64 个测试全过，闸门全过。

日报里鸣潮那块改成人话：「刷 无音区·玄幽东岳 ×2（双倍）」，产出写它固定掉的
两个声骸套装「羽落空尘之歌、清邪荡煞之心」；明日安排同样写名字。对照表只有
核对过的五个无音区，没核对的会明写「对照表没登记」，不编。

今早两个「失败」都是误判，中继改成按证据判：
· 终末地那趟 17 个任务全部完成（基质刷取在清波寨刷了 3 次），只因上游把「据点交易」
  改了显示名，AUTO-MAS 对不上就记失败。中继现在看 MaaEnd 自己的日志：每个任务有始有终、
  没有一条失败，就是做完了。
· 鸣潮那趟日常做完了，退出时少写一句收尾，AUTO-MAS 记成「在完成任务前退出」。
  中继现在认 OK-WW 自己写的「Daily Task Completed」。
· 顺手修：日报里终末地「失败 X」那一段从来没显示过（正则少了多行标志）；
  OK-WW 补丁「叠了 2 层」是把上游新加的一行算成了我们的，改成只数我们自己的标记。
