把中继按功能拆开了，行为一个字没改：

· 记账/告警、漏跑检查、日报、关机判定各自一个文件；关机判定现在会先说清
  「关不关、为什么、卡在哪道门」，再去执行。
· 开机流程拆成十个步骤函数，顺序一目了然。
· OK-WW 的补丁一个补丁一个文件。
· 代码里堆着的事故经过搬到了文档，代码只留一句为什么。

拆完逐个函数比对过：除了关机判定那处有意的改写，其余一字不差；
既有的 64 个测试全过，闸门全过。
