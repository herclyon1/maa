这次是把中继整个通读了一遍之后的修复，不加功能。

修好的：
· 「跳过队列」「现在跑一趟」「待办里开关队列」从 09-02 起每次调用都会崩，
  原因是代码里一个变量名和模块名撞了。四天里这三个功能等于没有。
· 中继每 30 秒那一轮里，前面一段出错会把后面的日报和自动关机一起带走
  （09-04 就这样开了一上午）。现在每一段各自兜住，一段坏了后面照跑。
· 账本里一条时间写坏的记录，原来会让整天的日报发不出去、机器等日报开一夜。
  现在这种行跳过并写日志。
· 每一轮要问十来次「有没有游戏在跑」，每问一次起一个进程列表。现在三秒内复用，
  一轮省下几秒。
· 一处该用 pwsh 7 的地方用了旧版 PowerShell；一处在新版 Python 上会报警告的写法。

新加的闸门（本地）：pyflakes 静态检查进了仓库自检，撞名、未定义名、
先读后赋值这类错以后提交前就会被拦；并按机器上的 Python 3.14 严格编译。
