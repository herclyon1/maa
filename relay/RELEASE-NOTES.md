- 桌面上那个「中继关机开关」修好了：昨晚状态搬家之后它写的是中继已经不看的旧文件，按下去界面显示「不关机」、机器照样关，而且一声不吭。
- 写配置文件的五个地方原来各抄了一份「写临时文件再改名」，全都漏了最后一步落盘。这台机器每天硬断电两次，断在那一瞬间会留下一个空文件，而它们写的都是游戏脚本没有就起不来的配置。现在统一走同一份带落盘的实现。
- 待办文件里躺着 8 月 23 号的一条「开机后不要关机」。那个文件是版本号一变大就整批重放，下次改配置会把它一起执行，机器白开一夜。已清空。
