## 2026-09-04 手机页大重构

- 终末地和鸣潮原来那 10 项**改了不生效**：这两个脚本的「快速配置」是关的，
  AUTO-MAS 不把 MAS 里的字段下发给它们，它们看的是自己那份母本。
  现在这两段直接改母本，按下去是真生效。
- 补上基质刷取全套：使用刻写券、领取方式、最大循环次数、地区多选、战后筛选、任务开关。
- 撤掉不用改的：关卡模式、连战、任务序号；剿灭和残象聚落点位改成只读。
- 字段名一律用脚本自己的译名（原来那两个序号的标签是我编的，还编反了）。
