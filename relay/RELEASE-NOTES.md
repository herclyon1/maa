## 2026-09-01（待部署：机器关着）

- **预更新通知统一**：四个程序都写成「程序 已更新：旧版 → 新版」。
  MaaEnd 的旧版本改从 `interface.json` 读（08-30 那次启动 10 秒就装完重启，
  日志里根本没来得及出现旧版本行）；MAA 补上前后版本号，待装的包从
  包名里读目标版本；AUTO-MAS 改成「有更新：旧 → 新（安装中）」；
  旧版本真读不到时明说「（旧版本没读到）」，不省也不编。
- **队列改名**：「新队列」→「早班」，「Evening-MAA」→「晚班」。
  中继认旧名（`ark_relay/names.py`）：排队中的手机指令、跳过标记不受影响。
  AUTO-MAS 里的实际改名走 `scripts/mac/rename-queues.py`（需要机器开着）。

