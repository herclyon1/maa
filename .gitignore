.env
*.png
# 手机页的图标是发布产物，必须入库：gh-pages 是 cp web/* 过去的，
# 库里没有就等于线上缺文件（apple-touch-icon 404，主屏图标变成截图）。
!web/*.png
*.log
# 测试语料必须入库：回放测试和几个格式测试读的就是这些 .log。
# 2026-09-08 发现它们被 *.log 挡在库外——新克隆里 10 条回放全判失败、
# 两个测试直接崩，而代码一个字没错，人会照着假线索去查判定逻辑。
!relay/tests/fixtures/**/*.log
!relay/tests/replay/**/*.log
__pycache__/
.DS_Store

# 个人电脑的备份和还原点：放 ~/Claude/mac-backups/，别再被 git add -A 扫进公开仓库
.dock-backup-*.plist
.launcher-backup-*/
*.orig

# Fleet Monitor 编出来的可执行文件不入库：2026-09-08 发现库里那份比源码旧四天，
# 谁照它跑谁拿到的是没修过的版本。要用就跑 scripts/mac/build-fleetmonitor.sh 现编。
scripts/mac/FleetMonitor/FleetMonitor

